<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yns.wallet">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wallet"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ImportOrCreateWallet" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ImportActivity" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".DetailResetPwdActivity" />
        <activity android:name=".CreateStepImportActivity" />
        <activity android:name=".AddWalletActivity" />
        <activity android:name=".ReceiveActivity" />
        <activity android:name=".SendActivity" />
        <activity android:name=".SwapActivity" />
        <activity android:name=".AboutUsActivity" />
    </application>

</manifest>